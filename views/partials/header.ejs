<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="UIU Data Science Club - Empowering students with data science skills through community, learning, and opportunities.">
  <title><%= title %> | UIU Data Science Club</title>
  <!-- Modern fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header class="header" id="header">
    <div class="container">
      <div class="header-content">
        <a href="/" class="logo">
          <span class="logo-text">UIU DSC</span>
        </a>
        
        <nav class="nav" id="mainNav">
          <ul class="nav-list">
            <li><a href="/" class="nav-link <%= title === 'Home' ? 'active' : '' %>">Home</a></li>
            <li><a href="/events" class="nav-link <%= title === 'Events' ? 'active' : '' %>">Events</a></li>
            <li><a href="/team" class="nav-link <%= title === 'Our Team' ? 'active' : '' %>">Team</a></li>
            <li><a href="/partners" class="nav-link <%= title === 'Partners' ? 'active' : '' %>">Partners</a></li>
            <li><a href="/questions" class="nav-link <%= title === 'Questions Bank' ? 'active' : '' %>">Questions</a></li>
            <li><a href="/join" class="nav-link <%= title === 'Join Our Club' ? 'active' : '' %>">Join Us</a></li>
            <li><a href="/admin" class="nav-link <%= title === 'Admin Panel' ? 'active' : '' %>">Admin</a></li>
          </ul>
        </nav>

        <% if (title === 'Home') { %>
        <div id="weatherWidget" class="weather-widget">
          <img id="weatherIcon" class="weather-icon" src="" alt="Weather">
          <div class="weather-info">
            <span id="weatherTemp" class="weather-temp">--°C</span>
            <span id="weatherDesc" class="weather-desc">Loading...</span>
          </div>
        </div>
        <% } else { %>
        <a href="/join" class="btn btn-primary" style="padding: 0.625rem 1.5rem;">Join Now</a>
        <% } %>
        
        <button class="menu-toggle" id="menuToggle" onclick="toggleMenu()">
          <span></span>
          <span></span>
          <span></span>
        </button>
      </div>
    </div>
  </header>
