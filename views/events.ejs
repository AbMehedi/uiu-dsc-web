<%- include('partials/header') %>

<section class="section">
  <div class="container">
    <h2 class="section-title">Our Events</h2>
    <p class="section-subtitle">Join us for workshops, seminars, and exciting activities</p>

    <% if (upcomingEvents && upcomingEvents.length > 0) { %>
      <h3 style="font-size: 1.75rem; margin-bottom: 2rem; color: var(--primary-color);">Upcoming Events</h3>
      <div class="event-list">
        <% upcomingEvents.forEach(event => { %>
          <div class="event-card">
            <div class="event-image" style="background: linear-gradient(135deg, var(--primary-light), var(--secondary-color)); display: flex; align-items: center; justify-content: center; color: white; font-size: 3rem;">
              📅
            </div>
            <div class="event-content">
              <h3 class="event-title"><%= event.title %></h3>
              <div class="event-meta">
                <span class="event-meta-item">📅 <%= new Date(event.date).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></span>
                <span class="event-meta-item">🕒 <%= event.time %></span>
                <span class="event-meta-item">📍 <%= event.location %></span>
                <span class="event-meta-item">👥 <%= event.seats %> seats available</span>
              </div>
              <p class="event-description"><%= event.description %></p>
            </div>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="no-events" style="margin-bottom: 3rem;">
        <div class="no-events-icon">📅</div>
        <h3>No Upcoming Events</h3>
        <p>We're currently planning our next batch of exciting events. Check back soon!</p>
      </div>
    <% } %>

    <% if (pastEvents && pastEvents.length > 0) { %>
      <h3 style="font-size: 1.75rem; margin: 3rem 0 2rem; color: var(--text-secondary);">Past Events</h3>
      <div class="event-list">
        <% pastEvents.forEach(event => { %>
          <div class="event-card" style="opacity: 0.8;">
            <div class="event-image" style="background: linear-gradient(135deg, #94a3b8, #64748b); display: flex; align-items: center; justify-content: center; color: white; font-size: 3rem;">
              ✓
            </div>
            <div class="event-content">
              <h3 class="event-title"><%= event.title %></h3>
              <div class="event-meta">
                <span class="event-meta-item">📅 <%= new Date(event.date).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></span>
                <span class="event-meta-item">🕒 <%= event.time %></span>
                <span class="event-meta-item">📍 <%= event.location %></span>
              </div>
              <p class="event-description"><%= event.description %></p>
            </div>
          </div>
        <% }); %>
      </div>
    <% } %>
  </div>
</section>

<%- include('partials/footer') %>
